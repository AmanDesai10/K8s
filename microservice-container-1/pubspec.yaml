name: b00965752_container_1
description: A microservice that validates the input and passes it to the next microservice. 
version: 1.0.0

environment:
  sdk: ^3.2.3

dependencies:
  args: ^2.4.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  http: ^1.1.0
  lints: ^2.1.0
  test: ^1.24.0
